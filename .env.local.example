# Supabase Configuration
# 아래 값들은 Supabase 프로젝트 설정에서 확인할 수 있습니다.
# https://supabase.com/dashboard/project/[your-project]/settings/api

# Supabase Project URL
# Public 환경 변수 (브라우저에서 접근 가능)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase Anonymous Key
# Public 환경 변수 (브라우저에서 접근 가능)
# RLS(Row Level Security)가 적용된 요청에 사용
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase Service Role Key
# 서버 전용 (절대 클라이언트에 노출하지 마세요!)
# RLS를 우회하여 관리자 작업 수행 시 사용
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# 환경 변수 설정 방법:
# 1. 이 파일을 .env.local로 복사
# 2. Supabase 대시보드에서 값들을 복사하여 붙여넣기
# 3. .env.local 파일은 .gitignore에 포함되어 있어 Git에 커밋되지 않습니다
